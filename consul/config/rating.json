{
  "service": {
    "name": "rating-api",
    "port": 3000,
    "tags": ["web", "application", "urlprefix-/rating-api"],
    "check": {
      "id": "rating_api_status",
      "name": "Ratin Api Status",
      "tcp": "localhost:3000",
      "interval": "30s",
      "timeout": "20s"
    }
  }
}
