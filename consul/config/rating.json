{
  "service": {
    "name": "rating-api",
    "port": 8000,
    "tags": ["web", "application", "urlprefix-/rating-api"],
    "check": {
      "id": "rating_api_status",
      "name": "Rating Api Status",
      "tcp": "localhost:8000",
      "interval": "30s",
      "timeout": "20s"
    }
  }
}
